{
  "name": "canvas-gui-landing",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --port 3001 index.html --console",
    "build": "bun build index.html --outdir=dist --minify",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "canvas-gui": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
