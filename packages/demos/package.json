{
  "name": "canvas-gui-demos",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun index.html --console --port 3000",
    "build": "bun build index.html --outdir=dist --minify",
    "preview": "bunx serve dist",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "canvas-gui": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
